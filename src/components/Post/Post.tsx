
import React from 'react'
import TagIcon from '../TagIcon'
import { FaceBookIcon, ShareIcon, TwitterIcon, WhatsappIcon } from '@/assets/icons';
import Comments from '../comments';
import { formatPublishDate } from '@/utils/helper-functions';
import { ArticlePropsType } from '@/interface';

export default function Post({ data, title }: { data: ArticlePropsType, title: string }) {

    return (
       
    );
}
